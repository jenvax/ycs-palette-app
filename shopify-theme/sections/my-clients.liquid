{% assign palette_access_string = '' %}
{% if customer %}
  {% assign palette_access_string = customer.tags | join: ',' | upcase | prepend: ',' | append: ',' %}
{% endif %}

{% capture ycs_header_content %}{{ content_for_header }}{% endcapture %}
{% assign ycs_page_url = ycs_header_content
  | split: '"pageurl":"'
  | last
  | split: '"'
  | first
  | replace: '\/', '/'
  | replace: '\\u0026', '&'
  | replace: '%26', '&'
  | replace: '%3F', '?'
  | replace: '%3D', '='
  | append: '&'
%}

{% assign is_admin = false %}
{% if customer and palette_access_string contains ',YCS_ADMIN,' %}
  {% assign is_admin = true %}
{% endif %}
{% assign actual_is_admin = is_admin %}

{% assign is_trade_july_cohort = false %}
{% assign trade_july_cohort_expires_at = '2026-08-25 00:00:00 -0400' | date: '%s' %}
{% assign current_timestamp = 'now' | date: '%s' %}
{% if customer and palette_access_string contains ',TRADEJULYCOHORT,' and current_timestamp < trade_july_cohort_expires_at %}
  {% assign is_trade_july_cohort = true %}
{% endif %}

{% assign is_trade = false %}
{% if customer and palette_access_string contains ',TRADE,' %}
  {% assign is_trade = true %}
{% endif %}
{% if is_trade_july_cohort %}
  {% assign is_trade = true %}
{% endif %}

{% assign is_catool = false %}
{% if customer and palette_access_string contains ',CATOOL,' %}
  {% assign is_catool = true %}
{% endif %}

{% assign is_catool_growth = false %}
{% if customer and palette_access_string contains ',CATOOLGROWTH,' %}
  {% assign is_catool_growth = true %}
{% endif %}

{% assign admin_view_as = '' %}
{% if actual_is_admin %}
  {% if ycs_page_url contains 'viewAs=trade&' %}
    {% assign admin_view_as = 'trade' %}
  {% elsif ycs_page_url contains 'viewAs=catoolgrowth&' %}
    {% assign admin_view_as = 'catoolgrowth' %}
  {% elsif ycs_page_url contains 'viewAs=catool&' %}
    {% assign admin_view_as = 'catool' %}
  {% endif %}
{% endif %}

{% if admin_view_as != '' %}
  {% assign is_admin = false %}
  {% assign is_trade = false %}
  {% assign is_catool = false %}
  {% assign is_catool_growth = false %}

  {% if admin_view_as == 'trade' %}
    {% assign is_trade = true %}
  {% elsif admin_view_as == 'catoolgrowth' %}
    {% assign is_catool_growth = true %}
  {% elsif admin_view_as == 'catool' %}
    {% assign is_catool = true %}
  {% endif %}
{% endif %}

{% assign can_manage_clients = false %}
{% if is_admin or is_trade or is_catool or is_catool_growth %}
  {% assign can_manage_clients = true %}
{% endif %}

{% assign can_create_reports = false %}
{% if actual_is_admin or is_trade or is_catool or is_catool_growth %}
  {% assign can_create_reports = true %}
{% endif %}

<div
  class="ycs-clients"
  data-customer-id="{{ customer.id }}"
  data-can-manage-clients="{{ can_manage_clients }}"
  data-can-create-reports="{{ can_create_reports }}"
  data-is-admin="{{ actual_is_admin }}"
  data-is-trade="{{ is_trade }}"
  data-app-base-url="https://ycs-palette-app.vercel.app"
>
  {% if customer == nil %}
    <div class="ycs-clients__empty">
      <h1>Sign in to access My Clients</h1>
      <a href="{{ routes.account_login_url }}" class="ycs-clients__button">Sign In</a>
    </div>
  {% elsif can_manage_clients == false %}
    <div class="ycs-clients__empty">
      <h1>Access denied</h1>
      <p>This page is available with Trade, CATOOL, CATOOLGROWTH, and admin access.</p>
    </div>
  {% else %}
    <div class="ycs-clients__topbar">
      <nav class="ycs-clients__nav" aria-label="Color analysis tools">
        <a href="{{ routes.account_url }}" class="ycs-clients__back-link" data-ycs-leave-client-view>My Account</a>
        <a
          href="/pages/my-palettes?view=catools{% if admin_view_as != '' %}&viewAs={{ admin_view_as }}{% endif %}"
          class="ycs-clients__back-link"
          data-ycs-clients-back-link
          data-tools-href="/pages/my-palettes?view=catools{% if admin_view_as != '' %}&viewAs={{ admin_view_as }}{% endif %}"
          data-ycs-leave-client-view
        >Tools</a>
        <a
          href="/pages/my-clients{% if admin_view_as != '' %}?viewAs={{ admin_view_as }}{% endif %}"
          class="ycs-clients__back-link is-active"
          data-ycs-clients-nav-selected
          data-ycs-leave-client-view
          hidden
        >My Clients</a>
        <a
          href="/pages/photo-prep?mode=trade&workflow=color-analysis{% if admin_view_as != '' %}&viewAs={{ admin_view_as }}{% endif %}"
          class="ycs-clients__back-link"
          data-ycs-clients-nav-photo-prep
          data-ycs-leave-client-view
          hidden
        >Photo Prep</a>
        <a
          href="/pages/color-analysis-tool{% if admin_view_as != '' %}?viewAs={{ admin_view_as }}{% endif %}"
          class="ycs-clients__back-link"
          data-ycs-clients-nav-structured
          data-ycs-leave-client-view
          hidden
        >Structured Analysis</a>
        <a
          href="/pages/signature-color-analysis{% if admin_view_as != '' %}?viewAs={{ admin_view_as }}{% endif %}"
          class="ycs-clients__back-link"
          data-ycs-clients-nav-lip
          data-ycs-leave-client-view
          hidden
        >Lip & Draping Studio</a>
      </nav>
    </div>

    <div class="ycs-clients__header">
      <div>
        <p class="ycs-clients__eyebrow">Your Color Style</p>
        <h1 class="ycs-clients__title">My Clients</h1>
        <p class="ycs-clients__subtitle">View and manage your color analysis clients.</p>
      </div>
      <a href="/pages/my-clients?newClient=1{% if admin_view_as != '' %}&viewAs={{ admin_view_as }}{% endif %}" class="ycs-clients__button" data-ycs-add-client>Add Client</a>
    </div>

    {% if actual_is_admin %}
      <section class="ycs-clients__admin-grant" data-ycs-admin-palette-grant>
        <div>
          <p class="ycs-clients__eyebrow">Admin</p>
          <h2>Give Color Palette to Customer</h2>
        </div>
        <form class="ycs-clients__admin-grant-form" data-ycs-admin-palette-grant-form>
          <input class="ycs-clients__input" name="email" type="email" placeholder="Customer email" required>
          <select class="ycs-clients__input" name="paletteCode" required>
            <option value="">Color palette</option>
            <option value="CCL">Clear Cool Light</option>
            <option value="CCM">Clear Cool Medium</option>
            <option value="CCD">Clear Cool Deep</option>
            <option value="CWL">Clear Warm Light</option>
            <option value="CWM">Clear Warm Medium</option>
            <option value="CWD">Clear Warm Deep</option>
            <option value="SCL">Soft Cool Light</option>
            <option value="SCM">Soft Cool Medium</option>
            <option value="SCD">Soft Cool Deep</option>
            <option value="SWL">Soft Warm Light</option>
            <option value="SWM">Soft Warm Medium</option>
            <option value="SWD">Soft Warm Deep</option>
            <option value="CWLG">Clear Warm Light for Gray Hair</option>
            <option value="CWMG">Clear Warm Medium for Gray Hair</option>
            <option value="CWDG">Clear Warm Deep for Gray Hair</option>
            <option value="SWLG">Soft Warm Light for Gray Hair</option>
            <option value="SWMG">Soft Warm Medium for Gray Hair</option>
            <option value="SWDG">Soft Warm Deep for Gray Hair</option>
            <option value="LO">Light Olive</option>
            <option value="MO">Medium Olive</option>
            <option value="DO">Deep Olive</option>
          </select>
          <button class="ycs-clients__button" type="submit">Give Color Palette to Customer</button>
        </form>
        <div class="ycs-clients__admin-grant-status" data-ycs-admin-palette-grant-status hidden></div>
      </section>
    {% endif %}

    <div class="ycs-clients__toolbar" data-ycs-client-list-controls>
      <input
        type="search"
        class="ycs-clients__search"
        placeholder="Search by name or email"
        autocomplete="off"
        data-ycs-client-search
      />
      <select class="ycs-clients__select" data-ycs-client-palette-filter>
        <option value="all">All palettes</option>
        <option value="unassigned">Unassigned</option>
      </select>
      <select class="ycs-clients__select" data-ycs-client-sort>
        <option value="updated-desc">Recently Updated</option>
        <option value="newest">Newest</option>
        <option value="oldest">Oldest</option>
        <option value="first-name">First Name A-Z</option>
        <option value="last-name">Last Name A-Z</option>
      </select>
    </div>

    <div class="ycs-clients__status" data-ycs-client-status>Loading clients...</div>
    <div class="ycs-clients__grid" data-ycs-client-grid></div>
    <div class="ycs-clients__detail" data-ycs-client-detail hidden></div>
  {% endif %}
</div>

{{ 'ycs-my-clients.js' | asset_url | script_tag }}

<style>
  .shopify-section-header,
  #shopify-section-header,
  .site-header,
  .header-wrapper,
  .header,
  nav[role="navigation"],
  #shopify-section-footer,
  .shopify-section--footer,
  .site-footer,
  footer.footer,
  footer[role="contentinfo"] {
    display: none !important;
  }

  .bredcrumbWrap {
    display: none !important;
  }

  .ycs-clients {
    max-width: 1240px;
    margin: 0 auto;
    padding: 18px;
    color: #2f2a25;
  }

  .ycs-clients__topbar {
    margin-bottom: 10px;
  }

  .ycs-clients__nav {
    display: flex;
    flex-wrap: wrap;
    gap: 10px;
    align-items: center;
  }

  .ycs-clients__back-link {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    min-height: 38px;
    border: 1px solid #e6ded5;
    border-radius: 999px;
    background: #fff;
    color: #2f2a25;
    font-size: 14px;
    font-weight: 800;
    line-height: 1;
    padding: 0 18px;
    text-decoration: none;
  }

  .ycs-clients__back-link:hover,
  .ycs-clients__back-link.is-active {
    border-color: #111;
    background: #111;
    color: #fff;
  }

  .ycs-clients__header {
    display: flex;
    align-items: end;
    justify-content: space-between;
    gap: 18px;
    margin-bottom: 14px;
  }

  .ycs-clients__eyebrow {
    margin: 0 0 6px;
    color: #8b7d6b;
    font-size: 12px;
    letter-spacing: 0.08em;
    text-transform: uppercase;
  }

  .ycs-clients__title {
    margin: 0 0 6px;
    font-size: 28px;
    line-height: 1.08;
  }

  .ycs-clients__subtitle {
    margin: 0;
    color: #5f5a54;
    font-size: 14px;
  }

  .ycs-clients__admin-grant {
    border: 1px solid #eadfd9;
    border-radius: 8px;
    padding: 18px;
    margin-bottom: 22px;
    background: #fffaf8;
  }

  .ycs-clients__admin-grant h2 {
    margin: 2px 0 14px;
    font-size: 20px;
  }

  .ycs-clients__admin-grant-form {
    display: grid;
    grid-template-columns: minmax(180px, 1fr) minmax(180px, 1fr) auto;
    gap: 10px;
    align-items: center;
  }

  .ycs-clients__admin-grant-status,
  .ycs-clients__grant-status {
    margin-top: 12px;
    font-size: 14px;
    color: #554842;
  }

  .ycs-clients__toolbar {
    display: flex;
    gap: 12px;
    flex-wrap: wrap;
    margin-bottom: 18px;
  }

  .ycs-clients__search,
  .ycs-clients__select,
  .ycs-clients__input,
  .ycs-clients__textarea {
    min-height: 44px;
    border: 1px solid #ddd3c8;
    border-radius: 12px;
    background: #fff;
    color: #2f2a25;
    font: inherit;
    padding: 10px 14px;
  }

  .ycs-clients__search {
    flex: 1 1 320px;
  }

  .ycs-clients__status,
  .ycs-clients__empty {
    border: 1px solid #ece7df;
    background: #fffdf9;
    border-radius: 18px;
    padding: 24px;
    text-align: center;
    color: #625c55;
  }

  .ycs-clients__credit-status {
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    justify-content: center;
    gap: 14px;
  }

  .ycs-clients__credit-status p {
    margin: 0;
  }

  .ycs-clients__palette-credit-status {
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    justify-content: space-between;
    gap: 10px;
    border: 1px solid #d8eadb;
    border-radius: 8px;
    padding: 10px 12px;
    background: #f7fbf6;
    color: #416b4a;
    font-size: 13px;
    font-weight: 800;
  }

  .ycs-clients__palette-credit-status.is-empty {
    border-color: #eadfd9;
    background: #fffaf8;
    color: #5f5a54;
  }

  .ycs-clients__palette-credit-status p {
    margin: 0;
  }

  .ycs-clients__palette-credit-status .ycs-clients__button {
    flex: 0 1 auto;
    min-height: 34px;
    padding: 8px 14px;
    font-size: 12px;
  }

  .ycs-clients__grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(240px, 1fr));
    gap: 16px;
  }

  .ycs-client-card {
    border: 1px solid #ece7df;
    border-radius: 18px;
    background: #fff;
    overflow: hidden;
    box-shadow: 0 8px 24px rgba(0,0,0,.04);
  }

  .ycs-client-card__photo {
    position: relative;
    height: 150px;
    background: #f6f1ea;
    display: flex;
    align-items: center;
    justify-content: center;
    overflow: hidden;
  }

  .ycs-client-card__photo img {
    width: 100%;
    height: 100%;
    object-fit: contain;
    display: block;
  }

  .ycs-client-card__placeholder {
    width: calc(100% - 24px);
    height: calc(100% - 24px);
    border: 2px dashed #d8cfc3;
    border-radius: 14px;
    display: flex;
    align-items: center;
    justify-content: center;
    color: #8a8176;
    font-size: 14px;
    text-align: center;
    padding: 12px;
    background: #fffaf4;
  }

  a.ycs-client-card__placeholder {
    color: #2f2a25;
    font-weight: 800;
    text-decoration: none;
  }

  a.ycs-client-card__placeholder:hover {
    border-color: #111;
  }

  .ycs-client-card__content {
    padding: 14px;
  }

  .ycs-client-card__name {
    margin: 0 0 5px;
    font-size: 20px;
    line-height: 1.15;
  }

  .ycs-client-card__email,
  .ycs-client-card__date {
    margin: 0 0 8px;
    color: #746b62;
    font-size: 13px;
    line-height: 1.35;
    word-break: break-word;
  }

  .ycs-client-card__palette {
    margin: 0 0 10px;
    color: #2f2a25;
    font-size: 14px;
    font-weight: 800;
    line-height: 1.35;
  }

  .ycs-client-card__badges {
    display: flex;
    flex-wrap: wrap;
    gap: 7px;
    margin: 0 0 12px;
  }

  .ycs-client-badge {
    display: inline-flex;
    align-items: center;
    min-height: 24px;
    border-radius: 999px;
    padding: 0 9px;
    background: #f6f1ea;
    color: #5f5348;
    font-size: 12px;
    font-weight: 800;
  }

  .ycs-client-badge--palette {
    background: #f6f1ea;
    color: #5f5348;
  }

  .ycs-client-card__actions,
  .ycs-clients__detail-actions {
    display: flex;
    flex-wrap: wrap;
    gap: 8px;
    margin-top: 12px;
  }

  .ycs-clients__button,
  .ycs-client-card__button {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    min-height: 40px;
    border: 0;
    border-radius: 999px;
    background: #111;
    color: #fff;
    cursor: pointer;
    font: inherit;
    font-size: 13px;
    font-weight: 800;
    padding: 0 16px;
    text-decoration: none;
  }

  .ycs-client-card__button--secondary,
  .ycs-clients__button--secondary {
    background: #efe7dc;
    color: #2f2a25;
  }

  .ycs-client-card__button--danger,
  .ycs-clients__button--danger {
    background: #fff5f7;
    color: #bd3155;
    border: 1px solid #f2c8d3;
  }

  .ycs-clients__button--tertiary {
    background: transparent;
    color: #5f5a54;
    border: 0;
    padding-inline: 8px;
  }

  .ycs-clients__button[disabled] {
    cursor: not-allowed;
    opacity: .45;
  }

  .ycs-clients__detail {
    border: 1px solid #ece7df;
    border-radius: 18px;
    background: #fff;
    padding: 18px;
    box-shadow: 0 8px 24px rgba(0,0,0,.04);
  }

  .ycs-clients__detail-header {
    display: grid;
    grid-template-columns: 220px 1fr;
    gap: 24px;
    align-items: start;
  }

  .ycs-clients__detail-header--edit {
    gap: 24px;
  }

  .ycs-clients__detail-photo {
    min-height: 260px;
    border-radius: 14px;
    background: #f6f1ea;
    overflow: hidden;
    display: flex;
    align-items: center;
    justify-content: center;
  }

  button.ycs-clients__detail-photo,
  a.ycs-clients__detail-photo {
    width: 100%;
    border: 1px solid #e6ded5;
    color: inherit;
    cursor: pointer;
    font: inherit;
    padding: 0;
    text-decoration: none;
  }

  button.ycs-clients__detail-photo:hover,
  a.ycs-clients__detail-photo:hover {
    border-color: #111;
    background: #fbfaf8;
  }

  .ycs-clients__upload-photo-button {
    width: 100%;
    margin-top: 10px;
  }

  .ycs-clients__detail-photo img {
    width: 100%;
    height: 100%;
    object-fit: contain;
  }

  .ycs-clients__detail h2 {
    margin: 0;
    font-size: 30px;
    line-height: 1.12;
  }

  .ycs-clients__detail-main {
    display: grid;
    gap: 10px;
    max-width: 560px;
  }

  .ycs-clients__detail-main--edit {
    max-width: 980px;
  }

  .ycs-clients__detail-meta {
    color: #625c55;
    line-height: 1.35;
  }

  .ycs-clients__detail-meta--edit {
    font-size: 13px;
    line-height: 1.25;
    margin-top: -4px;
  }

  .ycs-clients__photo-list {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(180px, 1fr));
    gap: 12px;
    margin-top: 18px;
  }

  .ycs-clients__photo-list figure {
    margin: 0;
    border: 1px solid #ece7df;
    border-radius: 14px;
    overflow: hidden;
    background: #fffdf9;
  }

  .ycs-clients__photo-list img {
    width: 100%;
    height: 180px;
    object-fit: contain;
    display: block;
    background: #f6f1ea;
  }

  .ycs-clients__photo-list figcaption {
    padding: 9px 10px;
    font-size: 13px;
    font-weight: 800;
    color: #5f5348;
  }

  .ycs-client-photo-manager {
    margin-top: 18px;
  }

  .ycs-client-photo-manager[hidden] {
    display: none;
  }

  .ycs-client-photo-manager__panel {
    border: 1px solid #ece7df;
    border-radius: 16px;
    background: #fffdf9;
    padding: 16px;
  }

  .ycs-client-photo-manager__head {
    display: flex;
    align-items: flex-start;
    justify-content: space-between;
    gap: 12px;
    flex-wrap: wrap;
  }

  .ycs-client-photo-manager__head h3 {
    margin: 0 0 4px;
    font-size: 20px;
  }

  .ycs-client-photo-manager__head p,
  .ycs-client-photo-manager__status {
    margin: 0;
    color: #625c55;
    font-size: 13px;
  }

  .ycs-client-photo-manager__status {
    margin-top: 10px;
  }

  .ycs-client-photo-manager__status--error {
    color: #bd3155;
    font-weight: 800;
  }

  .ycs-client-photo-manager__select-all {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    margin-top: 14px;
    color: #5f5348;
    font-size: 13px;
    font-weight: 800;
  }

  .ycs-client-photo-manager__grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(180px, 1fr));
    gap: 12px;
    margin-top: 12px;
  }

  .ycs-client-photo-manager__card {
    position: relative;
    overflow: hidden;
    border: 1px solid #ece7df;
    border-radius: 14px;
    background: #fff;
  }

  .ycs-client-photo-manager__card img {
    display: block;
    width: 100%;
    height: 180px;
    object-fit: contain;
    background: #f6f1ea;
  }

  .ycs-client-photo-manager__check {
    position: absolute;
    top: 8px;
    left: 8px;
    display: inline-flex;
    align-items: center;
    gap: 5px;
    border-radius: 999px;
    background: rgba(255, 255, 255, .92);
    color: #2f2a25;
    font-size: 12px;
    font-weight: 800;
    padding: 5px 8px;
  }

  .ycs-client-photo-manager__body {
    display: grid;
    gap: 7px;
    padding: 10px;
  }

  .ycs-client-photo-manager__body strong {
    color: #2f2a25;
    font-size: 13px;
    line-height: 1.25;
  }

  .ycs-client-photo-manager__body small {
    color: #7a6e63;
    font-size: 12px;
  }

  .ycs-clients__edit-form {
    display: grid;
    gap: 14px;
    margin-top: 6px;
    max-width: 520px;
    border-top: 1px solid #ece7df;
    padding-top: 14px;
  }

  .ycs-clients__edit-layout {
    display: grid;
    grid-template-columns: minmax(320px, 520px) minmax(260px, 340px);
    gap: 20px;
    align-items: start;
    border-top: 1px solid #ece7df;
    padding-top: 14px;
  }

  .ycs-clients__edit-layout .ycs-clients__edit-form,
  .ycs-clients__edit-layout .ycs-clients__section {
    border-top: 0;
    margin-top: 0;
    padding-top: 0;
  }

  .ycs-clients__edit-form h3,
  .ycs-clients__section h3 {
    margin: 0;
    color: #2f2a25;
    font-size: 18px;
    line-height: 1.25;
  }

  .ycs-clients__edit-form .ycs-clients__input {
    min-height: 36px;
    margin: 0 !important;
    padding: 8px 12px;
  }

  .ycs-clients__field {
    display: grid;
    gap: 2px;
    margin: 0 !important;
  }

  .ycs-clients__field:has(.ycs-clients__field-help) {
    gap: 4px;
  }

  .ycs-clients__field > span,
  .ycs-clients__field > label {
    display: block;
    margin: 0 !important;
    color: #5f5348;
    font-size: 12px;
    font-weight: 900;
    letter-spacing: .04em;
    line-height: 1.2;
  }

  .ycs-clients__field.has-error .ycs-clients__input {
    border-color: #bd3155;
    box-shadow: 0 0 0 2px rgba(189, 49, 85, .12);
  }

  .ycs-clients__field-help {
    display: block;
    margin: 1px 0 0 !important;
    color: #7a6e63;
    font-size: 12px;
    font-weight: 700;
    line-height: 1.35;
  }

  .ycs-clients__field input + .ycs-clients__field-help {
    margin-top: 2px !important;
  }

  .ycs-clients__field-help.is-error {
    color: #bd3155;
  }

  .ycs-clients__shopify-tags {
    border: 1px solid #eadfd9;
    border-radius: 8px;
    padding: 12px;
    background: #fffaf8;
  }

  .ycs-clients__shopify-tags-title {
    margin: 0 0 8px;
    color: #8a8178;
    font-size: 12px;
    font-weight: 900;
    letter-spacing: .08em;
    text-transform: uppercase;
  }

  .ycs-clients__shopify-tag-list {
    display: flex;
    flex-wrap: wrap;
    gap: 8px;
  }

  .ycs-clients__shopify-tag-pill {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    min-height: 34px;
    border: 1px solid #ddd3c8;
    border-radius: 999px;
    padding: 4px 6px 4px 12px;
    background: #fff;
    color: #2f2a25;
    font-size: 13px;
    font-weight: 900;
  }

  .ycs-clients__shopify-tag-pill.is-pending-remove {
    background: #fff5f7;
    border-color: #f2c8d3;
    color: #8f2540;
    text-decoration: line-through;
  }

  .ycs-clients__shopify-tag-remove {
    min-width: 24px;
    min-height: 24px;
    border: 0;
    border-radius: 999px;
    padding: 0 7px;
    background: #efe7dc;
    color: #2f2a25;
    cursor: pointer;
    font: inherit;
    line-height: 1;
  }

  .ycs-clients__shopify-tag-pill.is-pending-remove .ycs-clients__shopify-tag-remove {
    background: #bd3155;
    color: #fff;
    text-decoration: none;
  }

  .ycs-clients__shopify-tags-help {
    margin: 8px 0 0;
    color: #5f5a54;
    font-size: 12px;
    font-weight: 700;
  }

  .ycs-clients__form-actions {
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    gap: 10px;
    margin-top: 2px;
  }

  .ycs-clients__form-actions .ycs-clients__button {
    flex: 0 1 auto;
  }

  .ycs-clients__form-actions .ycs-clients__button--tertiary {
    min-height: 34px;
    padding-inline: 10px;
  }

  .ycs-clients__textarea {
    min-height: 72px;
    margin: 0 !important;
    resize: vertical;
  }

  .ycs-clients__save-message {
    margin: 0;
    color: #4f8a5f;
    font-size: 13px;
    font-weight: 800;
  }

  .ycs-clients__section {
    display: grid;
    gap: 10px;
    max-width: 520px;
    border-top: 1px solid #ece7df;
    padding-top: 14px;
  }

  .ycs-clients__section-header {
    display: grid;
    gap: 4px;
  }

  .ycs-clients__section-header p {
    margin: 0;
    color: #625c55;
    font-size: 13px;
    line-height: 1.45;
  }

  .ycs-clients__assigned-palette {
    color: #2f2a25;
    font-size: 20px;
    font-weight: 800;
    line-height: 1.2;
  }

  .ycs-clients__palette-current {
    margin: 0;
    color: #2f2a25;
    font-size: 20px;
    font-weight: 800;
    line-height: 1.15;
  }

  .ycs-clients__palette-ready {
    margin: -2px 0 4px;
    color: #416b4a;
    font-size: 13px;
    font-weight: 800;
  }

  .ycs-clients__access-required {
    margin: -4px 0 0;
    color: #bd3155;
    font-size: 12px;
    font-weight: 800;
  }

  .ycs-clients__destructive-action {
    max-width: 520px;
    border-top: 1px solid #f2e3e7;
    padding-top: 12px;
  }

  .ycs-clients__text-danger {
    border: 0;
    background: transparent;
    color: #bd3155;
    cursor: pointer;
    font: inherit;
    font-size: 13px;
    font-weight: 800;
    padding: 0;
    text-decoration: underline;
    text-underline-offset: 3px;
  }

  .ycs-clients-discard {
    position: fixed;
    inset: 0;
    z-index: 9999;
    display: flex;
    align-items: center;
    justify-content: center;
    background: rgba(47, 42, 37, 0.45);
    padding: 18px;
  }

  .ycs-clients-discard__dialog {
    width: min(440px, 100%);
    border-radius: 18px;
    background: #fff;
    box-shadow: 0 18px 48px rgba(0,0,0,.18);
    padding: 22px;
  }

  .ycs-clients-discard__dialog h2 {
    margin: 0 0 8px;
    font-size: 26px;
  }

  .ycs-clients-discard__dialog p {
    margin: 0 0 18px;
    color: #625c55;
    font-size: 16px;
  }

  .ycs-clients-discard__actions {
    display: flex;
    flex-wrap: wrap;
    justify-content: flex-end;
    gap: 10px;
  }

  .ycs-report-builder {
    margin-top: 22px;
    border-top: 1px solid #ece7df;
    padding-top: 22px;
  }

  .ycs-report-builder__header {
    display: flex;
    align-items: flex-start;
    justify-content: space-between;
    gap: 16px;
    margin-bottom: 14px;
  }

  .ycs-report-builder__header h3 {
    margin: 0 0 6px;
    font-size: 24px;
    line-height: 1.15;
  }

  .ycs-report-builder__header p,
  .ycs-report-builder__status {
    margin: 0;
    color: #625c55;
    font-size: 14px;
  }

  .ycs-report-builder__actions {
    display: flex;
    flex-wrap: wrap;
    gap: 8px;
    justify-content: flex-end;
  }

  .ycs-report-builder__layout {
    display: grid;
    grid-template-columns: 154px minmax(250px, 320px) 1fr;
    gap: 18px;
    align-items: start;
    margin-top: 14px;
  }

  .ycs-report-form {
    display: grid;
    gap: 12px;
    max-height: 860px;
    overflow: auto;
    padding-right: 6px;
  }

  .ycs-report-form label {
    display: grid;
    gap: 6px;
    color: #4f463d;
    font-size: 13px;
    font-weight: 800;
  }

  .ycs-report-form__hint {
    margin: -6px 0 0;
    color: #776f67;
    font-size: 12px;
    line-height: 1.35;
  }

  .ycs-report-form input,
  .ycs-report-form select,
  .ycs-report-form textarea {
    width: 100%;
    min-height: 42px;
    border: 1px solid #ddd3c8;
    border-radius: 8px;
    background: #fff;
    color: #2f2a25;
    font: inherit;
    font-size: 14px;
    font-weight: 400;
    padding: 10px 12px;
  }

  .ycs-report-form textarea {
    min-height: 150px;
    resize: vertical;
    line-height: 1.45;
  }

  .ycs-report-form textarea.ycs-report-textarea--fill {
    height: calc(100vh - 280px);
    min-height: 520px;
  }

  .ycs-report-form-page {
    display: grid;
    gap: 12px;
  }

  .ycs-report-form-page__head {
    display: grid;
    gap: 2px;
    padding: 12px;
    border: 1px solid #e6ded5;
    border-radius: 8px;
    background: #f6f1ea;
  }

  .ycs-report-form-page__head span {
    color: #2f2a25;
    font-size: 13px;
    font-weight: 900;
    text-transform: uppercase;
  }

  .ycs-report-form-page__head small {
    color: #716960;
    font-size: 12px;
    font-weight: 600;
  }

  .ycs-report-form-panel {
    display: grid;
    gap: 10px;
    padding: 12px;
    border: 1px solid #e6ded5;
    border-radius: 8px;
    background: #fbfaf8;
  }

  .ycs-report-form-panel__head {
    display: grid;
    gap: 2px;
  }

  .ycs-report-form-panel__head span {
    color: #4f463d;
    font-size: 13px;
    font-weight: 800;
  }

  .ycs-report-form-panel__head small,
  .ycs-report-custom-pages__empty {
    color: #716960;
    font-size: 12px;
    font-weight: 500;
    line-height: 1.25;
  }

  .ycs-report-logo-controls__actions,
  .ycs-report-custom-pages__actions {
    display: flex;
    flex-wrap: wrap;
    gap: 8px;
  }

  .ycs-report-logo-controls__actions .ycs-clients__button,
  .ycs-report-custom-pages__actions .ycs-clients__button {
    min-height: 36px;
    padding: 8px 12px;
    font-size: 12px;
  }

  .ycs-report-custom-page {
    display: grid;
    gap: 10px;
    margin: 0;
    padding: 12px;
    border: 1px solid #ddd3c8;
    border-radius: 8px;
    background: #fff;
  }

  .ycs-report-custom-page legend {
    padding: 0 6px;
    color: #4f463d;
    font-size: 13px;
    font-weight: 800;
  }

  .ycs-report-custom-page__actions {
    display: flex;
    flex-wrap: wrap;
    gap: 8px;
  }

  .ycs-report-custom-page__actions .ycs-clients__button,
  .ycs-report-custom-page__actions .ycs-report-image-clear {
    min-height: 34px;
    padding: 7px 10px;
    font-size: 12px;
  }

  .ycs-report-checkbox {
    display: flex !important;
    grid-template-columns: none !important;
    align-items: center;
    gap: 8px !important;
  }

  .ycs-report-checkbox input {
    width: 18px;
    min-height: 18px;
    height: 18px;
    margin: 0;
    padding: 0;
  }

  .ycs-report-image-picker {
    display: grid;
    gap: 8px;
    padding: 10px;
    border: 1px solid #e6ded5;
    border-radius: 8px;
    background: #fbfaf8;
  }

  .ycs-report-image-picker__head {
    display: grid;
    gap: 2px;
  }

  .ycs-report-image-picker__trigger {
    display: grid;
    grid-template-columns: 86px minmax(0, 1fr);
    gap: 10px;
    align-items: center;
    width: 100%;
    border: 0;
    background: transparent;
    color: inherit;
    cursor: pointer;
    font: inherit;
    padding: 0;
    text-align: left;
  }

  .ycs-report-image-picker__trigger:hover .ycs-report-image-picker__preview {
    border-color: #2100b8;
    box-shadow: 0 0 0 2px rgba(33, 0, 184, .12);
  }

  .ycs-report-image-picker__head span {
    color: #4f463d;
    font-size: 13px;
    font-weight: 800;
  }

  .ycs-report-image-picker__head small {
    color: #716960;
    font-size: 12px;
    font-weight: 500;
    line-height: 1.25;
  }

  .ycs-report-image-picker__preview {
    width: 86px;
    height: 112px;
    border: 1px solid #e6ded5;
    border-radius: 7px;
    background: #fff;
    object-fit: contain;
    object-position: center bottom;
  }

  .ycs-report-image-picker__preview--empty {
    display: flex;
    align-items: center;
    justify-content: center;
    color: #8a8176;
    font-size: 12px;
    text-align: center;
  }

  .ycs-report-image-picker__choices {
    display: grid;
    gap: 8px;
  }

  .ycs-report-image-picker__choices summary {
    width: max-content;
    color: #2100b8;
    cursor: pointer;
    font-size: 12px;
    font-weight: 800;
  }

  .ycs-report-image-picker__choices[open] summary {
    margin-bottom: 8px;
  }

  .ycs-report-image-picker__grid {
    display: grid;
    grid-template-columns: repeat(3, minmax(0, 1fr));
    gap: 8px;
  }

  .ycs-report-image-option {
    display: grid;
    gap: 5px;
    width: 100%;
    min-width: 0;
    border: 2px solid transparent;
    border-radius: 8px;
    background: #fff;
    padding: 5px;
    color: #2f2a25;
    cursor: pointer;
    font: inherit;
    text-align: left;
  }

  .ycs-report-image-option:hover {
    border-color: #d8cec2;
  }

  .ycs-report-image-option.is-selected {
    border-color: #2100b8;
    box-shadow: 0 0 0 2px rgba(33, 0, 184, .12);
  }

  .ycs-report-image-option img {
    width: 100%;
    aspect-ratio: 3 / 4;
    border-radius: 6px;
    background: #f3eee7;
    object-fit: cover;
  }

  .ycs-report-image-option span {
    overflow: hidden;
    color: #4f463d;
    font-size: 11px;
    font-weight: 800;
    line-height: 1.15;
    text-overflow: ellipsis;
    white-space: nowrap;
  }

  .ycs-report-image-picker__empty {
    grid-column: 1 / -1;
    margin: 0;
    color: #716960;
    font-size: 12px;
  }

  .ycs-report-image-clear {
    justify-self: start;
    border: 0;
    background: transparent;
    color: #2100b8;
    cursor: pointer;
    font: inherit;
    font-size: 12px;
    font-weight: 800;
    padding: 0;
  }

  .ycs-report-cover-controls {
    display: grid;
    gap: 10px;
    padding: 10px;
    border: 1px solid #e6ded5;
    border-radius: 8px;
    background: #fbfaf8;
  }

  .ycs-report-cover-controls__head {
    display: grid;
    gap: 2px;
  }

  .ycs-report-cover-controls__head span {
    color: #4f463d;
    font-size: 13px;
    font-weight: 800;
  }

  .ycs-report-cover-controls__head small {
    color: #716960;
    font-size: 12px;
    font-weight: 500;
  }

  .ycs-report-cover-controls label {
    gap: 5px;
  }

  .ycs-report-cover-controls input[type="range"] {
    min-height: 24px;
    padding: 0;
  }

  .ycs-report-preview-image-button {
    all: unset;
    display: inline-grid;
    justify-items: center;
    cursor: pointer;
    font: inherit;
    text-align: center;
  }

  .ycs-report-preview-image-button:hover .ycs-report-comparison-image-space {
    outline: 2px solid #2100b8;
    outline-offset: 3px;
  }

  .ycs-report-image-modal[hidden] {
    display: none;
  }

  .ycs-report-image-modal {
    position: fixed;
    inset: 0;
    z-index: 2000;
    display: grid;
    place-items: center;
    padding: 24px;
  }

  .ycs-report-image-modal__backdrop {
    position: absolute;
    inset: 0;
    background: rgba(17, 17, 17, .45);
  }

  .ycs-report-image-modal__dialog {
    position: relative;
    z-index: 1;
    width: min(760px, 100%);
    max-height: min(760px, 88vh);
    overflow: auto;
    border-radius: 8px;
    background: #fff;
    box-shadow: 0 18px 55px rgba(0, 0, 0, .22);
    padding: 18px;
  }

  .ycs-report-image-modal__head {
    display: flex;
    align-items: flex-start;
    justify-content: space-between;
    gap: 14px;
    margin-bottom: 14px;
  }

  .ycs-report-image-modal__head h4 {
    margin: 0 0 4px;
    color: #2f2a25;
    font-size: 20px;
    line-height: 1.2;
  }

  .ycs-report-image-modal__head p {
    margin: 0;
    color: #716960;
    font-size: 13px;
  }

  .ycs-report-image-modal__head button {
    border: 1px solid #ddd3c8;
    border-radius: 999px;
    background: #fff;
    color: #2f2a25;
    cursor: pointer;
    font: inherit;
    font-size: 13px;
    font-weight: 800;
    padding: 8px 14px;
  }

  .ycs-report-image-modal__grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(120px, 1fr));
    gap: 12px;
  }

  .ycs-report-image-modal__option {
    display: grid;
    gap: 7px;
    min-width: 0;
    border: 2px solid transparent;
    border-radius: 8px;
    background: #fbfaf8;
    color: #2f2a25;
    cursor: pointer;
    font: inherit;
    padding: 7px;
    text-align: left;
  }

  .ycs-report-image-modal__option:hover {
    border-color: #2100b8;
  }

  .ycs-report-image-modal__option img {
    width: 100%;
    aspect-ratio: 3 / 4;
    border-radius: 6px;
    background: #f3eee7;
    object-fit: cover;
  }

  .ycs-report-image-modal__option span {
    overflow: hidden;
    font-size: 12px;
    font-weight: 800;
    line-height: 1.15;
    text-overflow: ellipsis;
    white-space: nowrap;
  }

  .ycs-report-image-modal__empty {
    margin: 0;
    color: #716960;
    font-size: 14px;
  }

  .ycs-report-preview {
    display: grid;
    gap: 18px;
    justify-items: center;
    overflow: hidden;
    padding: 8px;
    background: #f6f1ea;
  }

  .ycs-report-preview-shell {
    display: grid;
    gap: 10px;
    align-content: start;
    min-width: 0;
  }

  .ycs-report-page-rail {
    position: sticky;
    top: 16px;
    display: grid;
    gap: 10px;
    max-height: 860px;
    overflow: auto;
    padding: 8px;
    border: 1px solid #e6ded5;
    border-radius: 8px;
    background: #fbfaf8;
  }

  .ycs-report-page-rail__head span {
    color: #4f463d;
    font-size: 12px;
    font-weight: 900;
    letter-spacing: .08em;
    text-transform: uppercase;
  }

  .ycs-report-page-rail__list {
    display: grid;
    gap: 7px;
  }

  .ycs-report-page-rail__item {
    display: grid;
    grid-template-columns: 44px minmax(0, 1fr);
    align-items: center;
    gap: 6px;
    width: 100%;
    border: 1px solid #e6ded5;
    border-radius: 8px;
    background: #fff;
    padding: 5px;
  }

  .ycs-report-page-rail__item.is-active {
    border-color: #111;
    box-shadow: 0 0 0 1px #111;
  }

  .ycs-report-page-rail__item.is-dragging {
    opacity: .45;
  }

  .ycs-report-page-rail__item.is-drop-target {
    outline: 2px solid #111;
    outline-offset: 3px;
  }

  .ycs-report-page-rail__select,
  button.ycs-report-page-rail__item {
    display: grid;
    grid-template-columns: 44px minmax(0, 1fr);
    align-items: center;
    gap: 6px;
    width: 100%;
    border: 0;
    background: transparent;
    color: inherit;
    cursor: pointer;
    font: inherit;
    padding: 0;
    text-align: left;
  }

  .ycs-report-page-rail__select {
    grid-column: 1 / -1;
  }

  .ycs-report-page-rail__item:not(.is-locked) .ycs-report-page-rail__select {
    display: grid;
    grid-column: 1 / -1;
  }

  .ycs-report-page-rail__thumb {
    display: grid;
    place-items: center;
    width: 44px;
    aspect-ratio: 8.5 / 11;
    border: 1px solid #ddd3c8;
    border-radius: 4px;
    background: #fff;
    color: #2f2a25;
    font-size: 16px;
    font-weight: 900;
  }

  .ycs-report-page-rail__label {
    color: #4f463d;
    font-size: 11px;
    font-weight: 800;
    line-height: 1.2;
    min-width: 0;
  }

  .ycs-report-page-rail__actions {
    display: flex;
    flex-wrap: wrap;
    gap: 4px;
    grid-column: 2 / -1;
    min-width: 0;
  }

  .ycs-report-page-rail__actions button {
    border: 1px solid #ddd3c8;
    border-radius: 999px;
    background: #fff;
    color: #716960;
    cursor: pointer;
    font: inherit;
    font-size: 12px;
    font-weight: 800;
    line-height: 1.1;
    min-height: 28px;
    min-width: 28px;
    padding: 6px 8px;
  }

  .ycs-report-page-rail__actions button:disabled {
    cursor: default;
    opacity: .38;
  }

  .ycs-report-page-rail__actions .ycs-report-page-rail__delete {
    border-color: #d9b8ae;
    color: #9a3324;
  }

  .ycs-report-page-add {
    display: grid;
    gap: 7px;
  }

  .ycs-report-page-add__primary {
    width: 100%;
    min-height: 34px;
    border: 1px solid #111;
    border-radius: 999px;
    background: #111;
    color: #fff;
    cursor: pointer;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    font-size: 13px;
    font-weight: 800;
    line-height: 1;
    padding: 8px 10px;
  }

  .ycs-report-page-add__more summary {
    color: #716960;
    cursor: pointer;
    font-size: 11px;
    font-weight: 800;
    line-height: 1.2;
    list-style: none;
    text-align: center;
    text-decoration: underline;
    text-underline-offset: 2px;
  }

  .ycs-report-page-add__more summary::-webkit-details-marker {
    display: none;
  }

  .ycs-report-page-add__menu {
    display: grid;
    gap: 6px;
    width: 100%;
    margin-top: 7px;
  }

  .ycs-report-page-add__menu span {
    grid-column: 1 / -1;
    color: #716960;
    font-size: 10px;
    font-weight: 800;
    line-height: 1.25;
    text-transform: uppercase;
  }

  .ycs-report-page-add__menu button {
    width: 100%;
    height: auto;
    min-height: 34px;
    border: 1px solid #ddd3c8;
    border-radius: 6px;
    background: #fff;
    color: #2f2a25;
    cursor: pointer;
    font: inherit;
    font-size: 11px;
    font-weight: 800;
    padding: 7px 8px;
    text-align: left;
  }

  .ycs-report-page {
    --report-page-width: 612px;
    --report-page-height: 792px;
    position: relative;
    width: min(100%, var(--report-page-width));
    aspect-ratio: 8.5 / 11;
    background: #fff;
    color: #050505;
    padding: 128px 54px 58px;
    box-shadow: 0 8px 24px rgba(0,0,0,.08);
    overflow: hidden;
    font-family: Arial, Helvetica, sans-serif;
  }

  .ycs-report-preview:not(.ycs-report-export) .ycs-report-page {
    display: none;
  }

  .ycs-report-preview[data-active-report-page="1"] [data-report-page="1"],
  .ycs-report-preview[data-active-report-page="2"] [data-report-page="2"],
  .ycs-report-preview[data-active-report-page="3"] [data-report-page="3"],
  .ycs-report-preview[data-active-report-page="4"] [data-report-page="4"],
  .ycs-report-preview[data-active-report-page="5"] [data-report-page="5"],
  .ycs-report-preview[data-active-report-page="6"] [data-report-page="6"],
  .ycs-report-preview[data-active-report-page="7"] [data-report-page="7"],
  .ycs-report-preview[data-active-report-page="8"] [data-report-page="8"],
  .ycs-report-preview[data-active-report-page="9"] [data-report-page="9"],
  .ycs-report-preview[data-active-report-page="10"] [data-report-page="10"],
  .ycs-report-preview[data-active-report-page="11"] [data-report-page="11"],
  .ycs-report-preview[data-active-report-page="12"] [data-report-page="12"],
  .ycs-report-preview[data-active-report-page="13"] [data-report-page="13"],
  .ycs-report-preview[data-active-report-page="14"] [data-report-page="14"],
  .ycs-report-preview[data-active-report-page="15"] [data-report-page="15"],
  .ycs-report-preview[data-active-report-page="16"] [data-report-page="16"],
  .ycs-report-preview[data-active-report-page="17"] [data-report-page="17"],
  .ycs-report-preview[data-active-report-page="18"] [data-report-page="18"],
  .ycs-report-preview[data-active-report-page="19"] [data-report-page="19"],
  .ycs-report-preview[data-active-report-page="20"] [data-report-page="20"] {
    display: block;
  }

  .ycs-report-export .ycs-report-page {
    display: block !important;
    box-sizing: border-box;
    width: 612px;
    height: 792px;
    aspect-ratio: auto;
    box-shadow: none;
  }

  .ycs-report-export .ycs-report-print-sheet {
    position: relative;
    width: 8.5in;
    height: 11in;
    margin: 0 auto;
    overflow: hidden;
    background: #fff;
  }

  .ycs-report-export .ycs-report-print-sheet .ycs-report-page {
    position: relative;
    top: 0;
    left: 0;
    transform: scale(1.3333333333);
    transform-origin: top left;
  }

  .ycs-report-page h1 {
    margin: 0 0 24px;
    font-family: Georgia, "Times New Roman", serif;
    font-size: 42px;
    line-height: .98;
    font-weight: 400;
    text-align: center;
    text-transform: uppercase;
  }

  .ycs-report-page h2 {
    margin: 0 0 12px;
    font-size: 26px;
    line-height: 1.1;
    text-transform: uppercase;
  }

  .ycs-report-page h3 {
    margin: 0 0 18px;
    color: #2100b8;
    font-size: 18px;
    line-height: 1.2;
    text-transform: uppercase;
  }

  .ycs-report-page p {
    margin: 0 0 14px;
    font-size: 17px;
    line-height: 1.38;
  }

  .ycs-report-page footer {
    position: absolute;
    left: 54px;
    right: 54px;
    bottom: 22px;
    border-top: 1px solid #b9b9b9;
    padding-top: 12px;
    display: flex;
    justify-content: space-between;
    gap: 16px;
    color: #111;
    font-size: 13px;
    text-transform: uppercase;
  }

  .ycs-report-page footer span:last-child {
    margin-left: auto;
    text-align: right;
  }

  .ycs-report-page--cover {
    padding-top: 52px;
    text-align: left;
  }

  .ycs-report-cover-title {
    position: absolute;
    top: 116px;
    left: 54px;
    right: 54px;
    text-align: center;
  }

  .ycs-report-page--cover h1 {
    margin-bottom: 0;
    font-size: 52px;
    text-transform: none;
  }

  .ycs-report-page--cover h2 {
    margin: 0 0 4px;
    font-family: Arial, Helvetica, sans-serif;
    font-size: 28px;
    font-weight: 800;
    text-transform: none;
  }

  .ycs-report-page--cover p {
    margin: 0;
    font-size: 22px;
    line-height: 1.2;
  }

  .ycs-report-logo {
    position: absolute;
    top: 36px;
    left: 50%;
    transform: translateX(-50%);
    width: 120px;
    max-height: 72px;
    object-fit: contain;
    object-position: center top;
  }

  .ycs-report-logo--text {
    width: auto;
    max-width: 280px;
    color: #777;
    font-family: Arial, Helvetica, sans-serif;
    font-size: 16px;
    font-weight: 800;
    line-height: 1.15;
    text-align: center;
  }

  .ycs-report-kicker {
    font-family: Georgia, "Times New Roman", serif;
    font-size: 52px !important;
    line-height: 1.05 !important;
  }

  .ycs-report-cover-art {
    position: absolute;
    left: 50%;
    top: 236px;
    width: 500px;
    height: 250px;
    transform: translateX(-50%);
  }

  .ycs-report-cover-art__fan {
    position: absolute;
    left: 0;
    right: 0;
    bottom: 0;
    width: 100%;
    height: 250px;
    object-fit: contain;
    object-position: center bottom;
  }

  .ycs-report-cover-art__drape-frame {
    position: absolute;
    left: 50%;
    bottom: 0;
    width: 245px;
    height: 230px;
    transform: translateX(-50%);
    overflow: hidden;
    z-index: 2;
  }

  .ycs-report-cover-art__drape-frame.is-draggable {
    cursor: grab;
    touch-action: none;
    user-select: none;
  }

  .ycs-report-cover-art__drape-frame.is-dragging {
    cursor: grabbing;
  }

  .ycs-report-cover-art__drape {
    position: absolute;
    left: 50%;
    bottom: 0;
    width: 100%;
    height: 100%;
    transform: translateX(-50%) translate(var(--cover-photo-x, 0), var(--cover-photo-y, 0)) scale(var(--cover-photo-scale, 1));
    transform-origin: center bottom;
    object-fit: contain;
    object-position: center bottom;
  }

  .ycs-report-cover-meta {
    position: absolute;
    top: 510px;
    left: 54px;
    right: 54px;
    text-align: center;
  }

  .ycs-report-cover-meta h2 {
    font-size: 24px;
    line-height: 1.1;
  }

  .ycs-report-cover-meta p {
    font-size: 17px;
    line-height: 1.2;
  }

  .ycs-report-copy {
    max-width: 490px;
  }

  .ycs-report-copy--letter {
    padding-top: 0;
  }

  .ycs-report-page--letter {
    padding-top: 132px;
  }

  .ycs-report-page--letter .ycs-report-copy {
    max-width: 500px;
  }

  .ycs-report-page--letter p {
    margin-bottom: 12px;
    font-size: 14px;
    line-height: 1.28;
  }

  .ycs-report-custom-photo-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 16px;
    margin-bottom: 22px;
  }

  .ycs-report-custom-photo-grid--four {
    grid-template-columns: 1fr 1fr;
    gap: 12px 16px;
    margin-bottom: 0;
  }

  .ycs-report-page--custom-photos-four h1 {
    margin-top: -10px;
    margin-bottom: 17px;
  }

  .ycs-report-custom-photo-grid .ycs-report-preview-image-button {
    display: block;
    width: 100%;
  }

  .ycs-report-custom-photo-grid .ycs-report-preview-image-button:hover .ycs-report-preview__custom-photo {
    outline: 2px solid #2100b8;
    outline-offset: 3px;
  }

  .ycs-report-preview__custom-photo {
    width: 100%;
    height: 293px;
    border: 1px solid #d8d8d8;
    background: #fff;
    object-fit: contain;
    object-position: center bottom;
  }

  .ycs-report-page--custom-photos .ycs-report-preview__image--empty {
    height: 293px;
  }

  .ycs-report-custom-photo-grid--four .ycs-report-preview__custom-photo,
  .ycs-report-custom-photo-grid--four .ycs-report-preview__image--empty {
    height: 258px;
  }

  .ycs-report-copy--custom {
    max-width: none;
  }

  .ycs-report-copy--custom p {
    font-size: 13px;
    line-height: 1.3;
  }

  .ycs-report-two-column {
    display: grid;
    grid-template-columns: 1fr .95fr;
    gap: 32px;
  }

  .ycs-report-page--science .ycs-report-two-column {
    grid-template-columns: 250px minmax(0, 1fr);
    gap: 18px;
  }

  .ycs-report-page--science h2 {
    margin-bottom: 8px;
    font-size: 17px;
    line-height: 1.05;
    font-weight: 800;
  }

  .ycs-report-page--science h3 {
    margin-bottom: 10px;
    font-size: 13px;
    line-height: 1.15;
    font-weight: 800;
  }

  .ycs-report-page--science p {
    margin-bottom: 10px;
    font-size: 12px;
    line-height: 1.22;
  }

  .ycs-report-science-right {
    box-sizing: border-box;
    min-width: 0;
    border-left: 1px solid #b8b8b8;
    padding-left: 16px;
  }

  .ycs-report-system-title {
    margin-top: 24px !important;
    margin-bottom: 12px !important;
    font-size: 12px !important;
  }

  .ycs-report-steps {
    display: grid;
    gap: 13px;
    margin-top: 0;
  }

  .ycs-report-steps div {
    display: grid;
    grid-template-columns: 42px 1fr;
    column-gap: 10px;
    align-items: start;
  }

  .ycs-report-steps span {
    grid-row: span 3;
    width: 38px;
    height: 38px;
    border-radius: 50%;
    background: #2100b8;
    color: #fff;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    font-size: 14px;
  }

  .ycs-report-steps strong {
    font-size: 16px;
    line-height: 1.1;
    text-transform: uppercase;
  }

  .ycs-report-steps p {
    color: #2100b8;
    margin-bottom: 3px;
    font-size: 11px;
    line-height: 1.18;
  }

  .ycs-report-steps .ycs-report-step-copy {
    color: #050505;
    font-size: 10px;
    line-height: 1.2;
  }

  .ycs-report-preview__image,
  .ycs-report-preview__wheel,
  .ycs-report-preview__wide-image,
  .ycs-report-preview__drape {
    width: 100%;
    display: block;
    object-fit: contain;
  }

  .ycs-report-preview__wheel {
    width: min(100%, 220px);
    max-width: 100%;
    height: 245px;
    margin-left: 50%;
    margin-top: 12px;
    transform: translateX(-50%);
  }

  .ycs-report-preview__wide-image {
    height: 360px;
    margin-bottom: 28px;
  }

  .ycs-report-preview__drape {
    height: 250px;
    margin-bottom: 22px;
  }

  .ycs-report-preview__image--empty {
    border: 2px dashed #d8cfc3;
    background: #fffaf4;
    color: #8a8176;
    display: flex;
    align-items: center;
    justify-content: center;
    text-align: center;
    font-size: 14px;
  }

  .ycs-report-comparison-grid {
    display: grid;
    gap: 10px;
    margin: 4px 0 24px;
  }

  .ycs-report-comparison-grid--three {
    grid-template-columns: repeat(3, 1fr);
  }

  .ycs-report-comparison-grid--two {
    grid-template-columns: repeat(2, 1fr);
    max-width: none;
    margin-left: auto;
    margin-right: auto;
  }

  .ycs-report-comparison-grid figure {
    margin: 0;
    text-align: center;
  }

  .ycs-report-comparison-image-space {
    display: inline-flex;
    align-items: flex-end;
    justify-content: center;
    width: 160px;
    max-width: 160px;
    height: 192px;
    position: relative;
  }

  .ycs-report-comparison-image-space > img:not(.ycs-report-checkmark) {
    display: block;
    width: 100%;
    height: 100%;
    object-fit: contain;
    object-position: center bottom;
  }

  .ycs-report-comparison-grid .ycs-report-preview__image--empty {
    width: 160px;
    max-width: 100%;
    height: 192px;
    object-fit: contain;
    object-position: center bottom;
  }

  .ycs-report-comparison-image-space:has(.ycs-report-preview__image--empty) {
    display: inline-flex;
  }

  .ycs-report-comparison-grid--two {
    gap: 16px;
  }

  .ycs-report-comparison-grid--two .ycs-report-preview-image-button {
    display: block;
    width: 100%;
  }

  .ycs-report-comparison-grid--two .ycs-report-comparison-image-space {
    display: flex;
    width: 100%;
    max-width: none;
    height: 293px;
  }

  .ycs-report-comparison-grid--two .ycs-report-preview__image--empty {
    width: 100%;
    height: 293px;
  }

  .ycs-report-checkmark {
    position: absolute;
    top: 8px;
    left: -2px;
    z-index: 3;
    width: 38px;
    height: 38px;
    object-fit: contain;
  }

  .ycs-report-comparison-grid figcaption,
  .ycs-report-comparison-caption {
    display: block;
    margin-top: 8px;
    color: #111;
    font-size: 16px;
    line-height: 1.1;
  }

  .ycs-report-page--comparison-copy .ycs-report-copy-subheading {
    font-size: 16px !important;
    line-height: 1.18 !important;
  }

  .ycs-report-page--comparison-copy .ycs-report-copy p:not(.ycs-report-copy-subheading) {
    font-size: 14px;
    line-height: 1.28;
  }

  .ycs-report-copy-subheading {
    margin-bottom: 12px !important;
    font-size: 19px !important;
    line-height: 1.25 !important;
  }

  .ycs-report-cover-art--inline {
    position: relative;
    top: auto;
    left: auto;
    width: 440px;
    height: 220px;
    margin: 0 auto 24px;
    transform: none;
  }

  .ycs-report-cover-art--inline .ycs-report-cover-art__fan {
    height: 220px;
  }

  .ycs-report-cover-art--inline .ycs-report-cover-art__drape-frame {
    width: 216px;
    height: 204px;
  }

  [hidden] {
    display: none !important;
  }

  @media (max-width: 1100px) {
    .ycs-clients__edit-layout {
      grid-template-columns: minmax(320px, 1fr) minmax(240px, 320px);
    }

    .ycs-report-builder__layout {
      grid-template-columns: 154px minmax(0, 1fr);
    }

    .ycs-report-page-rail {
      grid-column: 1;
      grid-row: 1 / span 2;
    }

    .ycs-report-form,
    .ycs-report-preview-shell {
      grid-column: 2;
      width: 100%;
    }

    .ycs-report-form {
      max-height: none;
      padding-right: 0;
    }
  }

  @media (max-width: 900px) {
    .ycs-clients__edit-layout {
      grid-template-columns: 1fr;
    }
  }

  @media (max-width: 720px) {
    .ycs-clients__header,
    .ycs-clients__detail-header,
    .ycs-report-builder__header,
    .ycs-report-builder__layout {
      display: block;
    }

    .ycs-clients__admin-grant-form {
      grid-template-columns: 1fr;
    }

    .ycs-clients__button {
      margin-top: 14px;
    }

    .ycs-clients__detail-photo {
      margin-bottom: 16px;
    }

    .ycs-report-builder__actions {
      justify-content: flex-start;
      margin-top: 12px;
    }

    .ycs-report-preview {
      margin-top: 18px;
      max-height: none;
    }

    .ycs-report-page-rail {
      position: static;
      max-height: none;
      margin-bottom: 14px;
    }

    .ycs-report-page-rail__list {
      grid-template-columns: repeat(auto-fit, minmax(96px, 1fr));
    }
  }

  @media print {
    html,
    body {
      width: 8.5in;
      margin: 0 !important;
      padding: 0 !important;
      background: #fff !important;
    }

    body * {
      visibility: hidden;
    }

    .ycs-report-export,
    .ycs-report-export * {
      visibility: visible;
    }

    .ycs-report-export {
      display: block;
      width: 8.5in;
      margin: 0;
      max-height: none;
      overflow: visible;
      padding: 0;
      background: #fff;
      gap: 0;
    }

    .ycs-report-print-sheet {
      page-break-after: auto;
      break-after: auto;
      page-break-inside: avoid;
      break-inside: avoid;
    }

    .ycs-report-print-sheet:not(:last-child) {
      page-break-after: always;
      break-after: page;
    }

    .ycs-report-print-sheet .ycs-report-page {
      margin: 0;
      width: 612px;
      height: 792px;
      max-height: 792px;
      overflow: hidden;
      transform: scale(1.3333333333) !important;
      page-break-after: auto;
      break-after: auto;
      page-break-inside: avoid;
      break-inside: avoid;
    }

    @page {
      size: letter;
      margin: 0;
    }
  }
</style>

{% schema %}
{
  "name": "My Clients",
  "settings": [],
  "presets": [
    {
      "name": "My Clients"
    }
  ]
}
{% endschema %}
